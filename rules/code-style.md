# 代码风格规范

- ESLint + Prettier（必须）
- pnpm lint / format / build（提交前必须执行）
- 优先 Composition API
- 核心业务逻辑必须添加注释，说明“做什么 + 为什么这么做”
- 非直观的业务规则必须添加注释
- 公共函数（跨模块使用）必须使用 JSDoc 注释（包含参数和返回值说明）
- 简单函数或语义清晰的代码不强制添加注释，避免冗余
- 注释统一使用中文